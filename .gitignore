# 의존성
node_modules/
.pnpm-store/

# 빌드 산출물
dist/
*.tsbuildinfo
.turbo/

# Prisma 가 만들어내는 클라이언트 (스키마에서 다시 생성됩니다)
packages/database/generated/

# 비밀정보 — .env.example 만 커밋합니다
.env
.env.*
!.env.example

# 발급된 토큰 (액세스/리프레시 토큰이 평문으로 들어갑니다)
.tokens/
*.tokens.json

# 로그 / 캐시
logs/
*.log
npm-debug.log*
pnpm-debug.log*
coverage/
.vitest/
.eslintcache

# OS / 에디터
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# 로컬 실행 데이터 (JSON 파일 모드에서만 씁니다)
data/
